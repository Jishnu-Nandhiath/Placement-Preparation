class Solution {
public:
    int maxProfit(vector<int>& prices) {      
        
        index_of_element = distance(prices.begin(),find(prices.begin(),prices.end(),element));
        
    }
};
